from selenium import webdriver

driver = webdriver.Chrome(“insert the path to the webdriver”)

def find_element():

   driver.get(“https://www.keepsolid.com“)
   elem = driver.find_element_by_xpath(“.//*[@id=‘masthead’]//../a[contains(., ‘Products’)]“)
   elem.click()
   driver.close()

find_element()
